# ─────────────────────────────────────────────────────────────
# 2026 小X宝开源医疗社区黑客松 · 环境变量
#
# 用法:
#   1. 复制本文件:  cp .env.example .env.local
#   2. 按需修改下列值（任何变量都可省略，省略时使用代码内置 fallback）
#   3. 修改后需重启 dev / 重新 build 才会生效（Next.js 在构建期注入）
#
# 命名约定:
#   所有变量必须以 NEXT_PUBLIC_ 开头才能在客户端组件中读取。
#
# 优先级:
#   .env.local (本机, gitignored) > .env.production (生产) > .env (默认)
# ─────────────────────────────────────────────────────────────

# 站点公网地址（不要带尾部斜杠）
# 用途: sitemap.xml、robots.txt、Open Graph、Twitter Card、JSON-LD Event schema
# 影响页面: 全站 metadata 与 SEO
NEXT_PUBLIC_SITE_URL=https://hackathon.xiaoxbao.org

# 魔搭活动页正式 URL
# 用途: Hero 「前往魔搭报名」、Header 「立即报名」、Footer、CTA、JSON-LD offers.url
# 影响页面: 全站 CTA 按钮跳转目标
NEXT_PUBLIC_REGISTER_URL=https://modelscope.cn/

# 魔搭 Skill 创建入口
# 用途: /submit 页面「Skill 提交入口」按钮
NEXT_PUBLIC_SKILL_SUBMIT_URL=https://modelscope.cn/skills/create?template=custom

# 魔搭 MCP Server 创建入口
# 用途: /submit 页面「MCP 提交入口」按钮
NEXT_PUBLIC_MCP_SUBMIT_URL=https://modelscope.cn/mcp/servers/create
